for (int i = 0; i < canMsg.can_dlc; i++)  { // print the data
      if (i == 0) {
        CAN_MSG_ARR = canMsg.data[i];
      }
      if (i != 0) {
        CAN_MSG_ARR |= canMsg.data[i] << 8;
      }
    }